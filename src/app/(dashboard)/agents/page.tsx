import AgentsPage from '@/components/modules/agents/ui/agentsPage';
import React from 'react';

type Props = {};

const page = (props: Props) => {
  return (
    <AgentsPage />
  );
};

export default page;